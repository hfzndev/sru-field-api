{
  "name": "sru-field-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "SRU Field App — backend API + admin web (offline-first field data capture, IPAL Sulfur Cilacap)",
  "scripts": {
    "dev": "node scripts/version.js && next dev -p 3000",
    "build": "node scripts/version.js && next build",
    "start": "next start -p 3000",
    "test": "vitest run",
    "test:watch": "vitest",
    "version:write": "node scripts/version.js",
    "seed:admin": "node scripts/seed-admin.js",
    "seed:initial": "node scripts/seed-initial.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "better-sqlite3": "^12.2.0",
    "next": "^16.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22"
  }
}
